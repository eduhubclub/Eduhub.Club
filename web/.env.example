# Google Maps — World Clock city search (Geocoding + Time Zone APIs)
# Create a key in Google Cloud Console, enable those two APIs, then copy here.
# Restart the Vite dev server after changing this file.
VITE_GOOGLE_MAPS_API_KEY=
