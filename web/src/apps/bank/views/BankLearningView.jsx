import { MoneyWidget } from '../../mathTools/widgets/MoneyWidget';

/**
 * Bank → Learning — Money widget (hosted by MathTools).
 */
export function BankLearningView(props) {
  return <MoneyWidget {...props} />;
}
