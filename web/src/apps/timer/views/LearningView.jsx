import { ClockWidget } from '../../mathTools/widgets/ClockWidget';

/**
 * Timer → Learning — Clock widget (hosted by MathTools).
 */
export function LearningView(props) {
  return <ClockWidget {...props} />;
}
